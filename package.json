{
  "name": "workshop",
  "version": "1.0.0",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . Workshop --platform=win32 --arch=x64 --out=dist --overwrite",
    "build": "electron-builder",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "build": {
    "appId": "com.workshop.id",
    "productName": "Workshop",
    "directories": {
      "output": "dist"
    },
    "files": [
      "src/**/*",
      "dist/**/*",
      "node_modules/**/*",
      "package.json"
    ],
    "win": {
      "target": "nsis"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "sharp": "^0.33.4"
  },
  "engine": {
    "node": "20"
  },
  "devDependencies": {
    "electron": "^30.1.2",
    "electron-builder": "^24.13.3",
    "electron-packager": "^17.1.2"
  }
}
